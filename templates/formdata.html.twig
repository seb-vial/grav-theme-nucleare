{% extends 'partials/base.html.twig' %}

{% block content %}

    {{ content|raw }}

    <div class="alert">{{ form.message }}</div>
    <p>{{ 'THEME_NUCLEARE.FORM.SUMMARY'|t }}:</p>

    {% include "forms/data.html.twig" %}

{% endblock %}
